# Deep Learning Frameworks
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Intel GPU Support (Intel Iris Xe Graphics)
# Uncomment ONE of the following options to enable Intel GPU acceleration:
# 
# Option 1: DirectML (Recommended for Windows with Intel/AMD integrated GPUs)
torch-directml>=0.2.5.dev240914
# 
# Option 2: Intel Extension for PyTorch (For Linux or specific Intel Arc GPUs)
# Note: Not available via pip on Windows. Install from:
# https://intel.github.io/intel-extension-for-pytorch/index.html#installation
# intel-extension-for-pytorch
#
# Note: The notebook will work on CPU if no GPU acceleration is installed.
# GPU acceleration is optional but recommended for faster training.

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
Pillow>=9.5.0
scikit-learn>=1.2.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress Bars
tqdm>=4.65.0

# Jupyter Notebook Support
jupyter>=1.0.0
notebook>=6.5.0
ipykernel>=6.22.0
ipywidgets>=8.0.0

# Web Framework (Flask Application)
Flask>=2.3.0
flask-cors>=4.0.0
Werkzeug>=2.3.0

# Hugging Face API (Optional - for enhanced medical reports)
huggingface-hub>=0.20.0
requests>=2.31.0

# Kaggle API (for dataset download)
kaggle>=1.5.16

# Additional utilities
pathlib>=1.0.1
